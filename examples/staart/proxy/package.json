{
  "name": "proxy",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "bootstrap": "npm install && cp -n .env.dist .env && cp -n .env.docker.dist .env.docker",
    "dev": "nodemon .",
    "start": "node ."
  },
  "license": "MIT",
  "dependencies": {
    "dotenv": "^6.1.0",
    "express": "^4.16.4",
    "express-http-proxy": "^1.4.0"
  },
  "devDependencies": {
    "nodemon": "^1.18.6"
  }
}
